#to change the port number if the port is already in use in another application
#server.port=8081

#the path from /src/main/resources/META-INF/resources is taken care by spring the rest of the hierarchy is put in the prefix
spring.mvc.view.prefix=/WEB-INF/jsp/
spring.mvc.view.suffix=.jsp
logging.level.org.springframework=info
spring.datasource.url=jdbc:mysql://localhost:3306/todoschema
spring.datasource.username=root
spring.datasource.password=root
spring.jpa.hibernate.ddl-auto=update
